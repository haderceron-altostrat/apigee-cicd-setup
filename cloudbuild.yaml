steps:
- name: 'ubuntu'  # You can use any image that has echo, e.g., alpine, busybox
  entrypoint: 'bash'
  args:
  - '-c'
  - 'echo "Hello World from Cloud Build!"'

options:
  logging: CLOUD_LOGGING_ONLY
